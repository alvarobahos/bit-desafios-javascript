/* 12. Crear una función que reciba 1 número,
 imprimir si es negativo o si es positivo.*/

function posinega(a) {
  if (a >= 0) console.log(`positivo`);
  else console.log(`negativo`);
}
posinega(-5);
posinega(5);
